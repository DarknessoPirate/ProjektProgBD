<UserControl x:Class="ProjektProgBD.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjektProgBD.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Grid.Row="0" VerticalAlignment="Top">
                <Label Content="Username" Grid.Row="0" Grid.Column="0" Margin="10,0,0,0" HorizontalAlignment="Left"/>
                <TextBox Grid.Row="0" Grid.Column="0" Margin="10,0,0,0" Width="100" HorizontalAlignment="Left" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" VerticalAlignment="Top">
                <Label Content="Password" Grid.Row="0" Grid.Column="0" Margin="10,0,0,0" HorizontalAlignment="Left"/>
                <PasswordBox x:Name="PasswordBox" Grid.Row="0" Grid.Column="0" Margin="10,0,0,0" Width="100" HorizontalAlignment="Left"/>
            </StackPanel>

            <StackPanel Grid.Row="3" VerticalAlignment="Top">
                <Button Content="Log in" Grid.Row="2" Grid.Column="0" Width="70" Height="20" HorizontalAlignment="Left" Margin="10,10,0,0"
                        Command="{Binding LogInCommand}" CommandParameter="{Binding ElementName=PasswordBox}"></Button>
                <TextBlock  Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="20,5,0,0">
                    <Hyperlink Command="{Binding OpenRegistrationWindowCommand}">
                        Create an account
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
