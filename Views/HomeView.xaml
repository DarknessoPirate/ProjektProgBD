<UserControl x:Class="ProjektProgBD.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProjektProgBD.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
<<<<<<< HEAD

    <Grid>
        
        <TextBlock Text="{Binding CurrentUser.Name, Mode=TwoWay}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>

=======
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <TextBlock  Grid.Row="0" Grid.Column="2" Text="{Binding CurrentUser.Name, Mode=TwoWay}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="0" Grid.Column="0">
            <Label>Date:</Label>
            <TextBlock Text="{Binding CurrentDate}"/>
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1">
            <Label>Hour:</Label>
            <TextBlock Text="{Binding CurrentTime}"/>    
        </StackPanel>

    
    </Grid>
>>>>>>> 7d4931013f92bda206105987b95333696b20ccda
</UserControl>
